apiVersion: 1

datasources:
  - name: Garmin Stats
    uid: garmin-influxdb
    type: influxdb
    access: proxy
    url: http://influxdb:8181
    jsonData:
      version: InfluxQL
      dbName: ${INFLUXDB_BUCKET}
    isDefault: true
    editable: false
